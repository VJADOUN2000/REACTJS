var a = 50;
export var user = "Vikrant Singh"
export var skills =['frontend', 'backend','UI/UX']

export default a

// If we call or console any value by radom name the export default value will be called
//but if we use {} then we have to use the exact name of the variable is called